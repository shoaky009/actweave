[package]
name = "adapter-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
adapter-api = { path = "../adapter-api" }
automation = { path = "../automation" }
serde_json = "1"
tokio = { version = "1", features = ["rt", "time", "macros"] }

[dev-dependencies]
tokio = { version = "1", features = ["test-util"] }
