[package]
name = "demo-adapter"
version = "0.1.0"
edition = "2024"

[dependencies]
adapter-sdk = { path = "../adapter-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
adapter-runtime = { path = "../adapter-runtime" }
tokio = { version = "1", features = ["macros", "rt"] }
